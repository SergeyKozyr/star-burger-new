name: star-burger

include:
  - docker-compose.yml

services:
  backend:
    command: sh -c "python manage.py collectstatic --no-input && python manage.py runserver 0.0.0.0:8080"
    volumes:
      - ./backend:/opt/app
      - media:/opt/app/media
      - static:/opt/app/staticfiles